{{ if .Values.components.trustManager.enabled -}}

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "{{ .Values.global.cluster }}-trust-manager-netpol"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ .Values.global.project }}
  source:
    chart: {{ .Values.global.raw.helm.chart }}
    repoURL: {{ default .Values.global.helm.repository .Values.global.raw.helm.repository }}
    targetRevision: {{ .Values.global.raw.helm.targetRevision }}
    helm:
      releaseName: trust-manager-netpol
      values: |
        resources:
          - apiVersion: networking.k8s.io/v1
            kind: NetworkPolicy
            metadata:
              name: allow-same-namespace
              namespace: trust-manager-system
            spec:
              podSelector: {}
              policyTypes:
                - Ingress
                - Egress
              ingress:
                - from:
                  - podSelector: {}
              egress:
                - to:
                  - podSelector: {}
          - apiVersion: networking.k8s.io/v1
            kind: NetworkPolicy
            metadata:
              name: allow-ingress-from-cert-manager-to-trust-manager
              namespace: trust-manager-system
            spec:
              ingress:
              - from:
                - namespaceSelector:
                    matchLabels:
                      namespace.ssc-spc.gc.ca/purpose: system
                      kubernetes.io/metadata.name: cert-manager-system
                  podSelector:
                    matchLabels:
                      app.kubernetes.io/name: cert-manager
              podSelector:
                matchLabels:
                  app.kubernetes.io/name: trust-manager
              policyTypes:
                - Ingress
          - apiVersion: networking.k8s.io/v1
            kind: NetworkPolicy
            metadata:
              name: allow-egress-to-cert-manager-from-trust-manager
              namespace: trust-manager-system
            spec:
              egress:
                - to:
                  - namespaceSelector:
                      matchLabels:
                        namespace.ssc-spc.gc.ca/purpose: system
                        kubernetes.io/metadata.name: cert-manager-system
                    podSelector:
                      matchLabels:
                        app.kubernetes.io/name: cert-manager
              podSelector:
                matchLabels:
                  app.kubernetes.io/name: trust-manager
              policyTypes:
              - Egress
          - apiVersion: cilium.io/v2
            kind: CiliumNetworkPolicy
            metadata:
              name: allow-ingress-from-apiserver-to-trust-manager
              namespace: trust-manager-system
            spec:
              description: |
                API server reaches trust-manager webhooks.
              endpointSelector: {}
              ingress:
                - fromEntities:
                    - kube-apiserver
          - apiVersion: cilium.io/v2
            kind: CiliumNetworkPolicy
            metadata:
              name: allow-egress-to-apiserver-from-trust-manager
              namespace: trust-manager-system
            spec:
              description: |
                trust-manager reconciles trust bundles via the Kubernetes API.
                Ports 443 and 6443 cover both ways the API server is reached: the in-cluster
                kubernetes Service (443) and the API server's native secure port (6443).
              endpointSelector:
                matchLabels:
                  app.kubernetes.io/name: trust-manager
              egress:
                - toEntities:
                    - kube-apiserver
                  toPorts:
                    - ports:
                        - port: "443"
                        - port: "6443"
  destination:
    name: {{ .Values.global.cluster }}
    namespace: platform-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true

{{- end }}
